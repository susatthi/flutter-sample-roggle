name: flutter_sample_roggle
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  firebase: ^9.0.2
  firebase_core: ^1.15.0
  firebase_crashlytics: ^2.7.1
  roggle: ^0.1.0-dev.2

flutter:
  uses-material-design: true
